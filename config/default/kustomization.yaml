apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: cmetal
resources:
  - ../base

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: ironic
    behavior: create
    envs:
      - ironic.env
